/* ==========================================================================
   Home Page - Uses Unified CSS Architecture
   ========================================================================== */

/* Main container using shared layout pattern */
.home-container {
  /* Using shared page-container pattern */
  width: 100%;
  display: flex;
  overflow: auto;
  min-height: var(--layout-min-height-full);
  align-items: center;
  flex-direction: column;
  background-color: var(--color-neutral-light);
}

/* Content area using shared layout pattern */
.home-content {
  /* Using shared page-content pattern */
  width: 100%;
  display: flex;
  max-width: var(--layout-max-width);
  align-items: center;
  padding-top: var(--layout-padding-section);
  flex-direction: column;
  padding-bottom: var(--layout-padding-content);
  padding-left: var(--layout-padding-small);
  padding-right: var(--layout-padding-small);
}

/* Home-specific sections */
.home-hero {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4xl);
  gap: var(--space-3xl);
}

.home-hero-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.home-hero-title {
  font-size: var(--font-size-4xl);
  font-weight: 700;
  color: var(--color-primary);
  margin-bottom: var(--space-lg);
  line-height: var(--line-height-tight);
}

.home-hero-subtitle {
  font-size: var(--font-size-lg);
  color: var(--color-text-secondary);
  margin-bottom: var(--space-xl);
  line-height: var(--line-height-relaxed);
  max-width: 600px;
}

.home-hero-actions {
  display: flex;
  gap: var(--space-lg);
  flex-wrap: wrap;
}

.home-hero-robot {
  flex: 1;
  min-height: 400px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Features sections */
.home-features {
  width: 100%;
  margin: var(--space-4xl) 0;
}

/* CTA sections */
.home-cta {
  width: 100%;
  margin: var(--space-4xl) 0;
}

/* Steps sections */
.home-steps {
  width: 100%;
  margin: var(--space-4xl) 0;
}

/* Contact section */
.home-contact {
  width: 100%;
  margin: var(--space-4xl) 0;
}

/* Responsive design */
@media (max-width: 768px) {
  .home-hero {
    flex-direction: column;
    text-align: center;
    gap: var(--space-2xl);
  }
  
  .home-hero-content {
    align-items: center;
  }
  
  .home-hero-title {
    font-size: var(--font-size-3xl);
  }
  
  .home-hero-actions {
    justify-content: center;
    width: 100%;
  }
  
  .home-hero-robot {
    min-height: 300px;
  }
}

@media (max-width: 480px) {
  .home-hero-title {
    font-size: var(--font-size-2xl);
  }
  
  .home-hero-actions {
    flex-direction: column;
    width: 100%;
  }
  
  .home-hero-actions .btn {
    width: 100%;
  }
}
